# canario — Dependencias Python
# Instalación: pip install -r requirements.txt
#
# NOTA: Este archivo lista las dependencias de runtime.
# Las dependencias de desarrollo (testing, linting) no están incluidas.
#
# Dependencias/tooling externo de runtime:
#   poppler-utils  (D0/D1 PDF directo)
#   OCRmyPDF + Tesseract + spa/eng/osd traineddata (D2 OCR; se descubren como CLI
#   externos y NO se importan como dependencias Python de canario)
#
# Licencia de dependencias:
#   pyyaml:        MIT
#   requests:      Apache 2.0
#   beautifulsoup4: MIT
#   python-docx:   MIT
#   openpyxl:      MIT

pyyaml==6.0.1
requests==2.31.0
beautifulsoup4==4.12.3
python-docx==1.1.0
openpyxl==3.1.5
