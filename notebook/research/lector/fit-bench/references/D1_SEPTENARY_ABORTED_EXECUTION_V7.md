# D1 septenary semantic-audit execution — aborted/incomplete

The fresh v7 reviewer returned `REFERENCE_DISPUTE` but could not truthfully complete the full septenary contract.

The preliminary 61-unit source ledger was sealed before v7 was opened, then found after reference access to have omitted the material request `Que diga qué fue lo que encontró.` even though v7 correctly represents it as `D1-F0795`. Therefore the clean source-ledger anti-anchoring claim is invalid and `reference_anchoring_contamination=true`; prior-review history remained unopened (`history_anchoring_contamination=false`).

Seven hard reverse defects were established before the run stopped. The supervising author then source-reopened the reviewer's named forward candidates and confirmed three additional hard groups: two omissions (`D1-SA-0090`, `D1-SA-0100`) and one missing reason/qualifier on `D1-F0785`.

This artifact is a **supervising record of an aborted reviewer execution**, not a completed septenary review and not reviewer-native Git delivery. Unadjudicated potential event/program rows are not promoted into findings.

D1 v7 remains `SUPERVISOR_DRAFT`; no independence gate is satisfied.
