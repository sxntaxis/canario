# Civic Processor Bench

State: **PARTIAL — natural corpus acquired; D1 reproduced; D2-D5 availability-limited.**

This directory is the mandatory evidence gate between the processor SOTA research package and any
production `WORKBENCH-001` freeze/implementation. It is deliberately outside `actakit/`: benchmark
adapters are not production processors.

## Principle

The bench answers two different questions with two kinds of civic material:

1. **Controlled civic derivatives** provide stable ground truth. The first fixture is page 2 of the
   official TSE Puntarenas alcaldías PDF already preserved in the repository. Clean-scan, low-DPI and
   skew/noise variants are generated from that page so the textual truth is known independently of OCR.
2. **Natural civic artifacts** expose real-world surprises. Esparza shadow-custody PDFs/HTML/DOCX and
   later other municipalities must be run without changing the benchmark contract to fit them.

Neither class alone is sufficient.

## What this bench may do

- invoke candidate tools in a disposable benchmark environment;
- render/generate controlled variants under `work/`;
- record exact backend/model/version/config identity;
- measure text fidelity, false insertions, table fidelity, latency/resource signals and egress;
- invoke explicitly authorized cloud candidates using host-provided secrets;
- write non-secret result artifacts under this research directory.

## What it may not do

- create `actakit/processors` or freeze production processor interfaces;
- modify `0001` or semantic schema;
- store API keys/tokens in Git, SQLite, results, logs or ProcessRun material;
- treat Tesseract/Docling/VLM self-confidence as a universal comparable probability;
- claim a winner before the minimum civic scenario matrix has enough natural + controlled coverage.

## Current controlled fixture

`TSE alcaldías Puntarenas, page 2 (Esparza)` is an official public civic page with a simple table and
known text. The manual ground truth was transcribed from the official page rendering and cross-checked
against the pre-SQL selector proof; it is not generated by Poppler/Tesseract.

The source PDF hash is fixed:

```text
192da0e99878aa310a906f381f3bb25c9678934743b1b7563df747e05a8eb4f3
```

## Run the local D1/D2 mechanics baseline

Benchmark-only dependencies:

```bash
python -m pip install -r notebook/research/workbench/processors/bench/requirements.txt
```

System tools currently exercised:

```text
pdftoppm / pdftotext (Poppler)
tesseract with spa language data
```

Run:

```bash
BENCH=notebook/research/workbench/processors/bench
python "$BENCH/validate_bench.py"
python "$BENCH/generate_controlled_variants.py" \
  --source notebook/research/pre-sql/fixtures/artifact-proofs/alcaldias_pu.pdf \
  --source-sha256 192da0e99878aa310a906f381f3bb25c9678934743b1b7563df747e05a8eb4f3 \
  --page 2 \
  --work-dir "$BENCH/work/control-tse"
PYTHONPATH="$BENCH" python "$BENCH/run_local_baselines.py" \
  --repo-root . \
  --work-dir "$BENCH/work/control-tse" \
  --result "$BENCH/results/local-controlled-baseline.json"
```

`work/` is disposable and ignored. Results may be committed when their environment and corpus identity
are fully recorded.

## Metrics are evidence, not policy

The current runner records normalized CER/WER, token precision/recall, required-span recall, exact table
row recovery, elapsed time, best-effort subprocess peak RSS and Tesseract word-confidence statistics.
These measurements **do not yet freeze escalation thresholds**. The final decision table is written only
after the broader corpus and D3-D5 candidates are measured.

## Cloud/OpenAI bench rule

OpenAI is a first-class cloud D5 candidate, but the bench must require an explicit host model and secret.
Do not hard-code a default API key or silently choose a model. A cloud result must record provider/model,
request-template identity, input pages/bytes sent, latency, reported usage/cost inputs and the applicable
retention/data-control profile. The secret value is never recorded.

An OpenAI-compatible endpoint is evaluated as a transport capability profile, not assumed feature parity.

The benchmark intentionally keeps provider invocation separate from scoring. A host-side OpenAI/Mistral/compatible runner may use its secret in process memory/environment, write only the derived text into `work/`, then `record_cloud_text_run.py` computes the same truth metrics and produces a non-secret evidence record. This prevents the benchmark recorder itself from becoming a credential store or premature production provider abstraction.

## Gate remains open

The natural increment adds real Esparza PDF/DOCX/HTML plus FECOMUDI and Quepos
actas. Natural fixtures are unscored until independent truth is curated. This
host could not execute D2-D5: exact blockers and `NOT_RUN_*` statuses are in
`results/natural-corpus-d1-and-availability.json`. The controlled local baseline
and natural D1 inventory prove the **bench**, not the processor stack.
